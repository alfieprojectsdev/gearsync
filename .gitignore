# Gradle
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar

# Android / IDE
local.properties
*.iml
.idea/
.cxx/
captures/

# OS
.DS_Store

# Build artifacts — never commit binaries; distribute via GitHub Releases
# (reproducible from source, and APKs bloat git history / don't delta-compress).
*.apk
gearsync-testdrive-fusion.pdf

# Release signing — NEVER commit the keystore or its credentials.
keystore.properties
*.jks
*.keystore
